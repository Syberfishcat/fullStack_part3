{
  "name": "ex1-phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Qisheng Yuan",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && cd ../frontend && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.5.0",
    "morgan": "^1.10.1"
  }
}
